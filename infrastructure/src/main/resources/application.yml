server:
  port: ${application.server.port}
spring:
  profiles:
    active:
      - spring-cloud
      - ${application.vendor.database}
      - ${application.vendor.messaging}

application:
  name: storage-api
  server:
    context-path: /api/storage
    port: ${STORAGE_SERVER_PORT:8080}
  vendor:
    database: ${STORAGE_VENDOR_DATABASE:postgres}
    messaging: ${STORAGE_VENDOR_MESSAGING:rabbitmq}
  messaging:
    consumer:
      file-upload-session-canceled-event:
        concurrency: ${STORAGE_CONSUMER_FILE_UPLOAD_SESSION_CANCELED_CONCURRENCY:1}
        max-attempts: ${STORAGE_CONSUMER_FILE_UPLOAD_SESSION_CANCELED_MAX_ATTEMPTS:1}
      file-upload-session-marked-for-deletion-event:
        concurrency: ${STORAGE_CONSUMER_FILE_UPLOAD_SESSION_MARKED_FOR_DELETION_CONCURRENCY:1}
        max-attempts: ${STORAGE_CONSUMER_FILE_UPLOAD_SESSION_MARKED_FOR_DELETION_MAX_ATTEMPTS:1}
